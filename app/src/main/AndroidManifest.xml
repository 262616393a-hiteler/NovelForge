<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.NovelCraft">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:launchMode="singleTop"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.NovelCraft">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".SettingsActivity"
            android:exported="false"
            android:label="@string/settings_title"
            android:theme="@style/Theme.NovelCraft" />

        <activity
            android:name=".LibraryActivity"
            android:exported="false"
            android:label="@string/library_title"
            android:theme="@style/Theme.NovelCraft" />

        <activity
            android:name=".ChapterContentActivity"
            android:exported="false"
            android:label="@string/chapter_content_title"
            android:theme="@style/Theme.NovelCraft" />

        <activity
            android:name=".ChapterSelectActivity"
            android:exported="false"
            android:label="@string/btn_review"
            android:theme="@style/Theme.NovelCraft" />

        <activity
            android:name=".OutlineActivity"
            android:exported="false"
            android:label="@string/outline_title"
            android:theme="@style/Theme.NovelCraft" />

        <activity
            android:name=".ChatActivity"
            android:exported="false"
            android:label="@string/btn_chat"
            android:theme="@style/Theme.NovelCraft"
            android:windowSoftInputMode="adjustResize" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>
