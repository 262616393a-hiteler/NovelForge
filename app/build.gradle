plugins {
    id("com.android.application")
}

android {
    namespace = "com.novelcraft.writer"
    compileSdk = 35

    defaultConfig {
        applicationId = "com.novelcraft.writer"
        minSdk = 24
        targetSdk = 35
        versionCode = 36
        versionName = "2.9.1"
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
        debug {
            minifyEnabled false
        }
    }

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_17
        targetCompatibility = JavaVersion.VERSION_17
    }
}

dependencies {
    implementation 'androidx.core:core:1.12.0'
}
